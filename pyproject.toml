[project]
name = "phoneapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dale", email = "dalesikkema@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
phoneapp = "phoneapp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
